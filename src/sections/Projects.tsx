export default function Projects() {
    return (
        <section id="projects" className="relative flex flex-col min-h-screen overflow-hidden pt-12">
            <h1 className="text-6xl font-bold text-blue-500 w-full text-center xl:ml-96 2xl:text-7xl xl:text-start 2xl:ml-112">Projects</h1>
            {/* Idea for project section: have some sort of cards showing key facts, when they click on one (or scroll), full page is taken over?*/}
            {/* Projects to use: Eosus, Tavio, WalkMini, March Madness Predictor*/}
            {/* What ill need: colors for each project (look through old stuff), info, code snippets, and a way to make it look unique*/}
        </section>
    );
}